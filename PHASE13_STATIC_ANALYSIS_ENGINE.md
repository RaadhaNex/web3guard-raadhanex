# Phase 13 — Real Static Analysis Engine

See `docs/PHASE13_STATIC_ANALYSIS_ENGINE.md` for full details.

Core rule: no fake tool output. Slither/Aderyn/Semgrep findings appear only when the backend actually runs the installed tool and parses its output.
