import { AccuracyUpgradeClient } from "@/components/accuracy-upgrade/AccuracyUpgradeClient";

export const dynamic = "force-dynamic";
export const metadata = {
  title: "Accuracy Upgrade | Web3Guard AI",
  description: "Phases 52–58 real evidence engines for dependency, static, API, wallet, business logic, DeFi simulation, and reviewed report confirmation.",
};

export default function AccuracyUpgradePage() {
  return <AccuracyUpgradeClient />;
}
