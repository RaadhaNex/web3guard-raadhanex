import { RouteLoadingShell } from "@/components/ui/RouteLoadingShell";

export default function AgencyLaunchLoading() {
  return <RouteLoadingShell title="Loading agency launch layer" description="Preparing client portfolio, intake, white-label, and handoff workspace..." />;
}
