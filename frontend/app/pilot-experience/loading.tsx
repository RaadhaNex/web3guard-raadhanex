import { RouteLoadingShell } from "@/components/ui/RouteLoadingShell";

export default function Loading() {
  return (
    <RouteLoadingShell
      label="Loading pilot experience cleanup"
      title="Preparing the first-user journey polish"
      description="Loading the seven-path journey, setup-state copy, feedback intake, and safe claim checks."
    />
  );
}
