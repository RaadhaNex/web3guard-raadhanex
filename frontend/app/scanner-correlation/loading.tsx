import { CommandLoadingState } from "@/components/ui/CommandState";

export default function ScannerCorrelationLoading() {
  return <CommandLoadingState label="Loading scanner correlation engine..." />;
}
